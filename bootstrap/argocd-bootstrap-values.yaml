global:
  logging:
    format: text

configs:
  params:
    server.insecure: true

server:
  service:
    type: ClusterIP
  extraArgs:
    - --insecure

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  replicas: 1

dex:
  enabled: false

notifications:
  enabled: false
